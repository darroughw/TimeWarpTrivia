<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Marks this as a TV app for the Play Store / Amazon Appstore and
         puts it on the Android TV / Fire TV (Leanback) home screen.
         required="true" means neither store will offer this to
         phones/tablets at all — this app is nothing but a WebView pointed
         at the TV-only /tv route, so a phone install would just be
         confusing. touchscreen required="false" also doubles as the
         signal Amazon's submission checker (TIM-43) uses to confirm this
         targets remote-only Fire TV devices, which have no touch input at
         all. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.TimeWarpTrivia"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.TimeWarpTrivia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- The one that actually matters for Android TV — without
                     this category the app installs fine but never shows up
                     as a launchable tile on the TV home screen. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
