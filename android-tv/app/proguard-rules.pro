# Referenced from app/build.gradle.kts's release buildType. Currently unused
# in practice (isMinifyEnabled is false — this app is a single Activity
# wrapping a WebView, nothing worth shrinking/obfuscating yet), kept as a
# starting point for if that ever changes.
